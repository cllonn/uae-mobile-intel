# Core data & geospatial
duckdb==1.5.5
pandas==3.0.5
numpy==2.5.2
geopandas==1.1.4
pyogrio==0.13.0
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.1
h3==4.5.0
osmium==4.3.1

# ML
scikit-learn==1.7.2

# Mapping
folium==0.20.0

# GenAI (optional -- only needed to connect a real LLM to src/copilot.py; without it, the
# copilot runs in deterministic template-narration mode, and the semantic intent-classification
# fallback in route() is simply unavailable (falls back to the same "could not map" message a
# deterministic miss already gives) -- zero external calls either way without this installed and
# ANTHROPIC_API_KEY set)
anthropic==0.69.0

# Notebook execution (VS Code / Jupyter)
ipykernel==7.3.0
nbformat==5.11.0
nbclient==0.11.0
nbconvert==7.17.1
